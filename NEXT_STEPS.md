# ✅ Следующие шаги для настройки vkbouwmaster.com

## ⚠️ ВАЖНО: Сначала нужно зарегистрировать домен!

Если вы еще не купили домен `vkbouwmaster.com`, сначала зарегистрируйте его. 
См. подробную инструкцию в файле `DOMAIN_REGISTRATION_GUIDE.md`

## 🎯 Рекомендация: Cloudflare Registrar

**Самый простой вариант** - зарегистрировать домен прямо в Cloudflare:
1. Зайдите на https://www.cloudflare.com/products/registrar/
2. Введите `vkbouwmaster.com`
3. Завершите покупку (~$9-10/год)
4. Домен автоматически настроится в Cloudflare!

## Текущий статус:
- ✅ Домен добавлен в Cloudflare (но еще не зарегистрирован)
- ⚠️ Nameservers еще не обновлены (поэтому видно "Invalid nameservers")
- ⏳ DNS записи будут созданы автоматически после настройки туннеля

## Шаг 1: Регистрация домена

**Если регистрируете в Cloudflare Registrar:**
- Домен автоматически настроится
- Переходите к Шагу 2

**Если регистрируете в другом регистраторе (Namecheap, GoDaddy и т.д.):**
- После покупки обновите Nameservers (см. ниже)

## Шаг 1.1: Обновите Nameservers у регистратора домена (если не Cloudflare Registrar)

**Ваши Cloudflare nameservers:**
1. `ashley.ns.cloudflare.com`
2. `salvador.ns.cloudflare.com`

**Что делать:**
1. Зайдите на сайт регистратора домена (где вы покупали vkbouwmaster.com)
   - Это может быть: GoDaddy, Namecheap, Google Domains, или другой регистратор
   
2. Найдите раздел "DNS Settings" или "Nameservers" или "Управление DNS"

3. Замените текущие nameservers на:
   - `ashley.ns.cloudflare.com`
   - `salvador.ns.cloudflare.com`

4. Сохраните изменения

5. Подождите 1-24 часа (обычно работает за 1-2 часа)

**После обновления nameservers:**
- Предупреждение "Invalid nameservers" исчезнет
- Cloudflare начнет управлять DNS вашего домена

## Шаг 2: Настройка туннеля (после обновления nameservers)

После того, как nameservers обновятся, выполните в терминале:

```bash
# 1. Авторизация в Cloudflare
cloudflared tunnel login

# 2. Создание туннеля
cloudflared tunnel create vkbouwmaster

# 3. Настройка DNS записей (это автоматически создаст A записи)
cloudflared tunnel route dns vkbouwmaster vkbouwmaster.com
cloudflared tunnel route dns vkbouwmaster www.vkbouwmaster.com
```

## Шаг 3: Создание конфигурации туннеля

1. Узнайте UUID туннеля:
```bash
cloudflared tunnel list
```

2. Создайте файл `~/.cloudflared/config.yml`:
```yaml
tunnel: vkbouwmaster
credentials-file: ~/.cloudflared/[UUID-из-команды-above].json

ingress:
  - hostname: vkbouwmaster.com
    service: http://127.0.0.1:3457
  - hostname: www.vkbouwmaster.com
    service: http://127.0.0.1:3457
  - service: http_status:404
```

**Замените `[UUID-из-команды-above]` на реальный UUID!**

## Шаг 4: Запуск

1. Убедитесь, что HTTP сервер запущен:
```bash
PORT=3457 npm run dev
```

2. В другом терминале запустите туннель:
```bash
npm run tunnel:custom
```

## ⚠️ Важно:

- **Не добавляйте DNS записи вручную** в Cloudflare dashboard - туннель создаст их автоматически
- **Предупреждения о отсутствующих DNS записях** исчезнут после настройки туннеля
- **Сначала обновите nameservers**, потом настраивайте туннель

## 📝 Где найти настройки nameservers у популярных регистраторов:

- **GoDaddy:** My Products → Domains → Manage DNS → Nameservers
- **Namecheap:** Domain List → Manage → Advanced DNS → Nameservers
- **Google Domains:** DNS → Name servers
- **Reg.ru:** Управление доменами → DNS-серверы

После обновления nameservers вернитесь к шагу 2!

