# 🚀 Руководство по постоянному хостингу сайта

Чтобы сайт был доступен 24/7, нужно разместить его на облачном сервере. Вот несколько вариантов:

## Вариант 1: Vercel (Рекомендуется) ⭐

**Самый простой вариант для Next.js проектов**

### Преимущества:
- ✅ Бесплатный план с хорошими лимитами
- ✅ Автоматический деплой из GitHub
- ✅ Автоматический SSL сертификат
- ✅ CDN по всему миру
- ✅ Простая настройка домена

### Шаги:

1. **Создайте аккаунт на Vercel:**
   - Перейдите на https://vercel.com
   - Зарегистрируйтесь через GitHub

2. **Подготовьте проект:**
   ```bash
   # Убедитесь, что проект собирается
   npm run build
   ```

3. **Деплой:**
   - Нажмите "New Project" в Vercel
   - Подключите ваш GitHub репозиторий
   - Vercel автоматически определит Next.js
   - Нажмите "Deploy"

4. **Настройка домена:**
   - В настройках проекта добавьте домен `vkbouwmaster.com`
   - Обновите DNS записи в Cloudflare:
     - Тип: `CNAME`
     - Имя: `@` или `www`
     - Значение: `cname.vercel-dns.com`

5. **Переменные окружения (если нужны):**
   - В настройках проекта → Environment Variables
   - Добавьте необходимые переменные

### Стоимость: Бесплатно (до 100GB трафика/месяц)

---

## Вариант 2: VPS сервер (DigitalOcean/Hetzner)

**Больше контроля, но требует настройки**

### Преимущества:
- ✅ Полный контроль над сервером
- ✅ Можно запускать любые приложения
- ✅ Низкая стоимость ($5-10/месяц)

### Шаги:

1. **Создайте VPS:**
   - DigitalOcean: https://www.digitalocean.com
   - Hetzner: https://www.hetzner.com
   - Выберите Ubuntu 22.04, минимум 1GB RAM

2. **Подключитесь к серверу:**
   ```bash
   ssh root@ваш-ip-адрес
   ```

3. **Установите Node.js и PM2:**
   ```bash
   # Обновление системы
   apt update && apt upgrade -y
   
   # Установка Node.js 20
   curl -fsSL https://deb.nodesource.com/setup_20.x | bash -
   apt install -y nodejs
   
   # Установка PM2 (менеджер процессов)
   npm install -g pm2
   ```

4. **Загрузите проект:**
   ```bash
   # Установите Git
   apt install -y git
   
   # Клонируйте репозиторий
   git clone https://github.com/ваш-username/vk-bouwmaster.git
   cd vk-bouwmaster
   
   # Установите зависимости
   npm install
   
   # Соберите проект
   npm run build
   ```

5. **Запустите с PM2:**
   ```bash
   # Запуск в production режиме
   pm2 start npm --name "vkbouwmaster" -- start
   
   # Сохраните конфигурацию для автозапуска
   pm2 save
   pm2 startup
   ```

6. **Настройте Nginx (обратный прокси):**
   ```bash
   # Установка Nginx
   apt install -y nginx
   
   # Создайте конфигурацию
   nano /etc/nginx/sites-available/vkbouwmaster
   ```
   
   Вставьте:
   ```nginx
   server {
       listen 80;
       server_name vkbouwmaster.com www.vkbouwmaster.com;
   
       location / {
           proxy_pass http://localhost:3000;
           proxy_http_version 1.1;
           proxy_set_header Upgrade $http_upgrade;
           proxy_set_header Connection 'upgrade';
           proxy_set_header Host $host;
           proxy_cache_bypass $http_upgrade;
       }
   }
   ```
   
   ```bash
   # Активируйте конфигурацию
   ln -s /etc/nginx/sites-available/vkbouwmaster /etc/nginx/sites-enabled/
   nginx -t
   systemctl restart nginx
   ```

7. **Настройте SSL (Let's Encrypt):**
   ```bash
   apt install -y certbot python3-certbot-nginx
   certbot --nginx -d vkbouwmaster.com -d www.vkbouwmaster.com
   ```

8. **Обновите DNS в Cloudflare:**
   - Тип: `A`
   - Имя: `@`
   - Значение: IP адрес вашего VPS
   - Тип: `A`
   - Имя: `www`
   - Значение: IP адрес вашего VPS

### Стоимость: $5-10/месяц

---

## Вариант 3: Railway

**Простой деплой, похож на Vercel**

### Шаги:

1. Зарегистрируйтесь на https://railway.app
2. Создайте новый проект из GitHub
3. Railway автоматически определит Next.js
4. Добавьте домен в настройках

### Стоимость: $5/месяц (или бесплатно с ограничениями)

---

## Вариант 4: Render

**Альтернатива Vercel**

### Шаги:

1. Зарегистрируйтесь на https://render.com
2. Создайте новый Web Service
3. Подключите GitHub репозиторий
4. Настройте:
   - Build Command: `npm run build`
   - Start Command: `npm start`

### Стоимость: Бесплатно (с ограничениями)

---

## Рекомендация

**Для вашего случая лучше всего подходит Vercel:**
- Проще всего настроить
- Бесплатно
- Идеально для Next.js
- Автоматические обновления при push в GitHub

---

## После деплоя

1. **Обновите DNS записи в Cloudflare:**
   - Удалите старые записи для Cloudflare Tunnel
   - Добавьте новые записи согласно инструкции выбранного хостинга

2. **Проверьте работу:**
   - Откройте https://vkbouwmaster.com
   - Убедитесь, что все страницы работают

3. **Настройте автоматический деплой:**
   - При каждом push в GitHub → автоматический деплой
   - Не нужно ничего делать вручную

---

## Важные файлы для деплоя

Убедитесь, что в проекте есть:
- ✅ `package.json` с правильными скриптами
- ✅ `.gitignore` (не коммитьте `node_modules`, `.env.local`)
- ✅ Все зависимости указаны в `package.json`

---

## Вопросы?

Если возникнут проблемы при деплое, проверьте:
1. Логи сборки в панели хостинга
2. Переменные окружения
3. DNS записи (может потребоваться время на обновление)

