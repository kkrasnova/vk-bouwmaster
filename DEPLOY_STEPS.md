# 🚀 Пошаговая инструкция по деплою на Vercel

## Вариант 1: Через GitHub (Рекомендуется) ⭐

### Шаг 1: Создайте репозиторий на GitHub

1. Откройте https://github.com
2. Нажмите "New repository" (зелёная кнопка справа)
3. Название: `vk-bouwmaster` (или любое другое)
4. Выберите "Private" или "Public"
5. **НЕ** добавляйте README, .gitignore или лицензию (у нас уже есть)
6. Нажмите "Create repository"

### Шаг 2: Подключите локальный проект к GitHub

Выполните команды в терминале (замените `YOUR_USERNAME` на ваш GitHub username):

```bash
cd "/Users/anastasiia.krasnova/Desktop/Все мои проекты/VK BOUWMASTER/VK BOUWMASTER"

# Добавьте remote репозиторий
git remote add origin https://github.com/YOUR_USERNAME/vk-bouwmaster.git

# Отправьте код на GitHub
git branch -M main
git push -u origin main
```

**Если GitHub попросит авторизацию:**
- Используйте Personal Access Token вместо пароля
- Или используйте GitHub CLI: `gh auth login`

### Шаг 3: Деплой на Vercel

1. Откройте https://vercel.com
2. Нажмите "Sign Up" → выберите "Continue with GitHub"
3. Разрешите доступ к репозиториям
4. Нажмите "Add New Project"
5. Выберите репозиторий `vk-bouwmaster`
6. Vercel автоматически определит:
   - **Framework Preset:** Next.js ✅
   - **Build Command:** `npm run build` ✅
   - **Output Directory:** `.next` ✅
7. Нажмите "Deploy"

### Шаг 4: Настройка домена

1. После успешного деплоя откройте настройки проекта
2. Перейдите в раздел **"Domains"**
3. Добавьте домен: `vkbouwmaster.com`
4. Добавьте домен: `www.vkbouwmaster.com`
5. Vercel покажет DNS записи

### Шаг 5: Обновление DNS в Cloudflare

1. Откройте панель Cloudflare: https://dash.cloudflare.com
2. Выберите домен `vkbouwmaster.com`
3. Перейдите в **DNS** → **Records**
4. **Удалите** старые записи для Cloudflare Tunnel (если есть)
5. **Добавьте** новые записи:

   **Запись 1:**
   - Тип: `CNAME`
   - Имя: `@`
   - Целевой хост: `cname.vercel-dns.com`
   - Прокси: ✅ Включен (оранжевое облако)
   - TTL: Auto

   **Запись 2:**
   - Тип: `CNAME`
   - Имя: `www`
   - Целевой хост: `cname.vercel-dns.com`
   - Прокси: ✅ Включен (оранжевое облако)
   - TTL: Auto

6. Сохраните изменения
7. Подождите 5-10 минут для обновления DNS

### Шаг 6: Проверка

1. Откройте https://vkbouwmaster.com
2. Убедитесь, что сайт работает
3. Проверьте все страницы

---

## Вариант 2: Через Vercel CLI (Без GitHub)

Если не хотите использовать GitHub, можно задеплоить напрямую:

### Шаг 1: Установите Vercel CLI

```bash
npm install -g vercel
```

### Шаг 2: Войдите в Vercel

```bash
vercel login
```

### Шаг 3: Деплой

```bash
cd "/Users/anastasiia.krasnova/Desktop/Все мои проекты/VK BOUWMASTER/VK BOUWMASTER"
vercel
```

Следуйте инструкциям:
- Set up and deploy? **Y**
- Which scope? Выберите ваш аккаунт
- Link to existing project? **N**
- What's your project's name? `vk-bouwmaster`
- In which directory is your code located? `./`
- Want to override the settings? **N**

### Шаг 4: Production деплой

```bash
vercel --prod
```

### Шаг 5: Настройка домена

1. Откройте https://vercel.com/dashboard
2. Выберите проект
3. Перейдите в **Settings** → **Domains**
4. Добавьте `vkbouwmaster.com` и `www.vkbouwmaster.com`
5. Обновите DNS в Cloudflare (как в Варианте 1, Шаг 5)

---

## После деплоя

✅ Сайт будет доступен 24/7
✅ При каждом `git push` → автоматический деплой (если использовали GitHub)
✅ Не нужно запускать локальный сервер
✅ Не нужно запускать Cloudflare Tunnel

## Остановка локальных серверов

После успешного деплоя можете остановить:

```bash
# Найти и остановить Next.js сервер
lsof -ti:3457 | xargs kill

# Остановить Cloudflare Tunnel (если запущен)
# Нажмите Ctrl+C в терминале где запущен tunnel
```

---

## Проблемы?

**Если деплой не работает:**
1. Проверьте логи в Vercel Dashboard
2. Убедитесь, что `npm run build` проходит успешно локально
3. Проверьте переменные окружения (если нужны)

**Если DNS не обновляется:**
1. Подождите до 24 часов (обычно 5-10 минут)
2. Проверьте DNS записи в Cloudflare
3. Убедитесь, что прокси включен (оранжевое облако)

