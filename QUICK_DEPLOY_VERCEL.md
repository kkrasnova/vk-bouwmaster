# ⚡ Быстрый деплой на Vercel (5 минут)

## Шаг 1: Подготовка

1. Убедитесь, что проект на GitHub:
   ```bash
   git add .
   git commit -m "Prepare for deployment"
   git push
   ```

## Шаг 2: Деплой на Vercel

1. Откройте https://vercel.com
2. Нажмите "Sign Up" → выберите GitHub
3. Нажмите "Add New Project"
4. Выберите ваш репозиторий `vk-bouwmaster`
5. Vercel автоматически определит настройки:
   - Framework Preset: Next.js
   - Build Command: `npm run build`
   - Output Directory: `.next`
6. Нажмите "Deploy"

## Шаг 3: Настройка домена

1. После деплоя откройте настройки проекта
2. Перейдите в "Domains"
3. Добавьте `vkbouwmaster.com` и `www.vkbouwmaster.com`
4. Vercel покажет DNS записи, которые нужно добавить

## Шаг 4: Обновление DNS в Cloudflare

1. Откройте панель Cloudflare для домена `vkbouwmaster.com`
2. Удалите старые записи для Cloudflare Tunnel (если есть)
3. Добавьте новые записи:
   - **Тип:** `CNAME`
   - **Имя:** `@`
   - **Значение:** `cname.vercel-dns.com`
   - **Прокси:** Включен (оранжевое облако)
   
   - **Тип:** `CNAME`
   - **Имя:** `www`
   - **Значение:** `cname.vercel-dns.com`
   - **Прокси:** Включен (оранжевое облако)

4. Подождите 5-10 минут для обновления DNS

## Шаг 5: Проверка

1. Откройте https://vkbouwmaster.com
2. Убедитесь, что сайт работает
3. Проверьте все страницы

## Готово! 🎉

Теперь сайт будет доступен 24/7, и при каждом push в GitHub будет автоматически обновляться.

## Автоматические обновления

После настройки:
- Каждый `git push` → автоматический деплой
- Vercel сам собирает проект
- Обновления появляются за 1-2 минуты

## Остановка локального сервера

После успешного деплоя на Vercel вы можете остановить локальный сервер:
```bash
# Остановить Next.js сервер
# (найдите процесс и остановите его)

# Остановить Cloudflare Tunnel (если запущен)
# Нажмите Ctrl+C в терминале где запущен tunnel
```

Сайт будет работать на Vercel независимо от вашего компьютера!

