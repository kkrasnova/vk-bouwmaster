# 🌐 Пошаговая инструкция: Настройка vkbouwmaster.com в Cloudflare

## Шаг 1: Регистрация бесплатного аккаунта Cloudflare

1. **Откройте:** https://dash.cloudflare.com/sign-up
   - Или закройте страницу Enterprise и нажмите "Sign Up" в правом верхнем углу

2. **Зарегистрируйтесь:**
   - Введите email
   - Придумайте пароль
   - Подтвердите email

## Шаг 2: Добавление домена в Cloudflare

1. **После входа в панель:**
   - Нажмите большую кнопку **"Add a Site"** (Добавить сайт)

2. **Введите домен:**
   - Введите: `vkbouwmaster.com`
   - Нажмите "Add site"

3. **Выберите план:**
   - Выберите **"Free"** (бесплатный план) - этого достаточно!
   - Нажмите "Continue"

4. **Проверьте DNS записи:**
   - Cloudflare автоматически найдет существующие DNS записи
   - Оставьте все как есть или добавьте нужные записи
   - Нажмите "Continue"

5. **Обновите Nameservers:**
   - Cloudflare покажет 2 nameserver адреса (например: `alice.ns.cloudflare.com` и `bob.ns.cloudflare.com`)
   - **ВАЖНО:** Скопируйте эти адреса
   - Идите к регистратору домена (где вы покупали vkbouwmaster.com)
   - Замените текущие nameservers на те, что дал Cloudflare
   - Это может занять до 24 часов, но обычно работает за 1-2 часа

## Шаг 3: Авторизация cloudflared в Cloudflare

Откройте терминал и выполните:

```bash
cloudflared tunnel login
```

- Откроется браузер
- Выберите аккаунт Cloudflare
- Разрешите доступ
- Вернетесь в терминал - авторизация завершена

## Шаг 4: Создание туннеля

```bash
cloudflared tunnel create vkbouwmaster
```

Вы увидите сообщение о создании туннеля и его UUID.

## Шаг 5: Настройка DNS записей для туннеля

```bash
cloudflared tunnel route dns vkbouwmaster vkbouwmaster.com
cloudflared tunnel route dns vkbouwmaster www.vkbouwmaster.com
```

Эти команды автоматически создадут DNS записи в Cloudflare.

## Шаг 6: Создание конфигурационного файла

1. **Узнайте UUID туннеля:**
   ```bash
   cloudflared tunnel list
   ```
   Скопируйте UUID туннеля `vkbouwmaster`

2. **Создайте конфигурацию:**
   ```bash
   mkdir -p ~/.cloudflared
   ```

3. **Создайте файл `~/.cloudflared/config.yml`:**
   
   Замените `[UUID]` на реальный UUID из шага 1:

   ```yaml
   tunnel: vkbouwmaster
   credentials-file: ~/.cloudflared/[UUID].json

   ingress:
     - hostname: vkbouwmaster.com
       service: http://127.0.0.1:3457
     - hostname: www.vkbouwmaster.com
       service: http://127.0.0.1:3457
     - service: http_status:404
   ```

   **Пример:**
   ```yaml
   tunnel: vkbouwmaster
   credentials-file: ~/.cloudflared/a1b2c3d4-e5f6-7890-abcd-ef1234567890.json

   ingress:
     - hostname: vkbouwmaster.com
       service: http://127.0.0.1:3457
     - hostname: www.vkbouwmaster.com
       service: http://127.0.0.1:3457
     - service: http_status:404
   ```

## Шаг 7: Запуск туннеля

**Убедитесь, что HTTP сервер запущен:**
```bash
PORT=3457 npm run dev
```

**В другом терминале запустите туннель:**
```bash
cd "/Users/anastasiia.krasnova/Desktop/Все мои проекты/VK BOUWMASTER/VK BOUWMASTER"
npm run tunnel:custom
```

Или:
```bash
cloudflared tunnel run vkbouwmaster
```

## Готово! 🎉

После настройки ваш сайт будет доступен по адресу:
- **https://vkbouwmaster.com**
- **https://www.vkbouwmaster.com**

## Важные моменты

✅ **Бесплатный план Cloudflare** - этого достаточно для туннеля  
✅ **Nameservers должны быть обновлены** у регистратора домена  
✅ **HTTP сервер должен работать** на порту 3457  
✅ **Туннель должен работать постоянно** для доступности сайта  

## Автозапуск туннеля (для постоянной работы)

Если хотите, чтобы туннель запускался автоматически:

```bash
sudo cloudflared service install
sudo systemctl enable cloudflared
sudo systemctl start cloudflared
```

## Проверка статуса

- Проверить туннели: `cloudflared tunnel list`
- Проверить DNS: `cloudflared tunnel route dns list`
- Проверить конфигурацию: `cloudflared tunnel info vkbouwmaster`

